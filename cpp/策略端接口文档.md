# Sequence 实盘交易框架 - 策略端接口文档

> 版本: 1.0.0  
> 更新日期: 2024-12-13  
> 说明: 本文档描述了策略端可调用的所有接口，包括C++端和Python端

---

## 目录

1. [架构概述](#1-架构概述)
2. [已实现的OKX接口汇总](#2-已实现的okx接口汇总)
3. [Python策略端接口](#3-python策略端接口)
4. [C++ REST API接口](#4-c-rest-api接口)
5. [C++ WebSocket接口](#5-c-websocket接口)
6. [ZeroMQ通信协议](#6-zeromq通信协议)
7. [数据结构定义](#7-数据结构定义)
8. [使用示例](#8-使用示例)

---

## 1. 架构概述

```
┌─────────────────────────────────────────────────────────────────┐
│                         OKX 交易所                               │
│     REST API (下单/撤单/查询)  +  WebSocket (行情/订单推送)        │
└───────────────────────────────────┬─────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      C++ Trading Server                         │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ OKX REST API│  │OKX WebSocket │  │     ZmqServer        │   │
│  │  (下单执行)  │  │ (行情接收)   │  │  PUB 行情 (30-100μs) │   │
│  │             │  │              │  │  PULL 订单           │   │
│  │             │  │              │  │  PUB 回报            │   │
│  └─────────────┘  └──────────────┘  └──────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────┘
                                    │ IPC (Unix Socket)
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Python Strategy Client                       │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │TradingClient│  │   Strategy   │  │       Models         │   │
│  │ (ZMQ通信)   │  │  (策略基类)  │  │ TickerData/Order     │   │
│  └─────────────┘  └──────────────┘  └──────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 通信延迟

| 通道 | 协议 | 典型延迟 |
|------|------|----------|
| 行情分发 | ZeroMQ IPC (PUB-SUB) | 30-100 μs |
| 订单请求 | ZeroMQ IPC (PUSH-PULL) | 30-100 μs |
| 订单回报 | ZeroMQ IPC (PUB-SUB) | 30-100 μs |
| OKX REST API | HTTPS | 50-200 ms |
| OKX WebSocket | WSS | 10-50 ms |

---

## 2. 已实现的OKX接口汇总

### 2.1 交易接口 (Trade)

| 接口名称 | OKX Endpoint | C++ 实现 | Python 实现 | 状态 |
|----------|--------------|----------|-------------|------|
| 下单 | POST /api/v5/trade/order | ✅ `place_order()` | ✅ `place_order()` | ✅ 完成 |
| 批量下单 | POST /api/v5/trade/batch-orders | ❌ | ✅ `place_batch_orders()` | Python only |
| 撤单 | POST /api/v5/trade/cancel-order | ✅ `cancel_order()` | ✅ `cancel_order()` | ✅ 完成 |
| 批量撤单 | POST /api/v5/trade/cancel-batch-orders | ✅ `cancel_batch_orders()` | ✅ `cancel_batch_orders()` | ✅ 完成 |
| 修改订单 | POST /api/v5/trade/amend-order | ❌ | ✅ `amend_order()` | Python only |
| 批量修改订单 | POST /api/v5/trade/amend-batch-orders | ❌ | ✅ `amend_batch_orders()` | Python only |
| 查询订单 | GET /api/v5/trade/order | ✅ `get_order()` | ✅ `get_order()` | ✅ 完成 |
| 未成交订单 | GET /api/v5/trade/orders-pending | ✅ `get_pending_orders()` | ✅ `get_orders_pending()` | ✅ 完成 |
| 历史订单 | GET /api/v5/trade/orders-history | ❌ | ✅ `get_orders_history()` | Python only |

### 2.2 账户接口 (Account)

| 接口名称 | OKX Endpoint | C++ 实现 | Python 实现 | 状态 |
|----------|--------------|----------|-------------|------|
| 账户余额 | GET /api/v5/account/balance | ✅ `get_account_balance()` | ✅ `get_balance()` | ✅ 完成 |
| 持仓信息 | GET /api/v5/account/positions | ✅ `get_positions()` | ✅ `get_positions()` | ✅ 完成 |
| 历史持仓 | GET /api/v5/account/positions-history | ❌ | ✅ `get_positions_history()` | Python only |
| 可交易产品 | GET /api/v5/account/instruments | ✅ `get_account_instruments()` | ✅ `get_account_instruments()` | ✅ 完成 |
| 账单流水(7天) | GET /api/v5/account/bills | ❌ | ✅ `get_bills()` | Python only |
| 账单流水(3月) | GET /api/v5/account/bills-archive | ❌ | ✅ `get_bills_archive()` | Python only |

### 2.3 行情接口 (Market)

| 接口名称 | OKX Endpoint | C++ 实现 | Python 实现 | 状态 |
|----------|--------------|----------|-------------|------|
| 行情快照 | GET /api/v5/market/ticker | ❌ | ✅ `get_ticker()` | Python only |
| 产品信息 | GET /api/v5/public/instruments | ❌ | ✅ `get_instruments()` | Python only |
| K线数据 | GET /api/v5/market/candles | ✅ `get_candles()` | ❌ | C++ only |

### 2.4 WebSocket 频道

| 频道名称 | 类型 | 端点 | C++ 实现 | Python 实现 | 状态 |
|----------|------|------|----------|-------------|------|
| tickers | 公共 | public | ✅ `subscribe_ticker()` | ✅ `subscribe_ticker()` | ✅ 完成 |
| trades | 公共 | public | ✅ `subscribe_trades()` | ✅ `subscribe_trades()` | ✅ 完成 |
| trades-all | 公共 | business | ✅ `subscribe_trades_all()` | ✅ `subscribe_trades_all()` | ✅ 完成 |
| books / orderbook | 公共 | public | ✅ `subscribe_orderbook()` | ❌ | C++ only |
| **candle*** | 公共 | **business** | ✅ `subscribe_kline()` | ✅ `subscribe_candles()` | ✅ 完成 |
| **open-interest** | 公共 | public | ✅ `subscribe_open_interest()` | ✅ | ✅ 完成 |
| **mark-price** | 公共 | public | ✅ `subscribe_mark_price()` | ✅ | ✅ 完成 |
| orders | 私有 | private | ✅ `subscribe_orders()` | ✅ `subscribe_orders()` | ✅ 完成 |
| **sprd-orders** | 私有 | **business** | ✅ `subscribe_sprd_orders()` | ❌ | ✅ 完成 |
| **sprd-trades** | 私有 | **business** | ✅ `subscribe_sprd_trades()` | ❌ | ✅ 完成 |
| positions | 私有 | private | ✅ `subscribe_positions()` | ❌ | C++ only |
| account | 私有 | private | ✅ `subscribe_account()` | ✅ `subscribe_account()` | ✅ 完成 |

#### WebSocket 端点说明

| 端点类型 | URL路径 | 用途 |
|----------|---------|------|
| public | `/ws/v5/public` | 行情、深度、成交 |
| **business** | `/ws/v5/business` | **K线、trades-all** |
| private | `/ws/v5/private` | 订单、持仓、账户 |

> ⚠️ **重要**: K线频道必须使用 `business` 端点，不能使用 `public` 端点！

#### 支持的K线间隔

| 间隔 | 频道名称 | 说明 |
|------|----------|------|
| 1s | candle1s | 1秒 |
| 1m | candle1m | 1分钟 |
| 3m | candle3m | 3分钟 |
| 5m | candle5m | 5分钟 |
| 15m | candle15m | 15分钟 |
| 30m | candle30m | 30分钟 |
| 1H | candle1H | 1小时 |
| 2H | candle2H | 2小时 |
| 4H | candle4H | 4小时 |
| 6H | candle6H | 6小时 |
| 12H | candle12H | 12小时 |
| 1D | candle1D | 1天 |
| 2D | candle2D | 2天 |
| 3D | candle3D | 3天 |
| 5D | candle5D | 5天 |
| 1W | candle1W | 1周 |
| 1M | candle1M | 1月 |
| 3M | candle3M | 3月 |

UTC时区版本（后缀utc）: candle3Mutc, candle1Mutc, candle1Wutc, candle1Dutc 等

---

## 3. Python策略端接口

### 3.1 TradingClient 类

交易客户端，负责与C++服务器通信。

```python
from trading_client import TradingClient, Strategy

client = TradingClient(strategy_id="my_strategy")
```

#### 构造函数

```python
TradingClient(
    strategy_id: str = "default",
    market_addr: str = "ipc:///tmp/trading_md.ipc",
    order_addr: str = "ipc:///tmp/trading_order.ipc",
    report_addr: str = "ipc:///tmp/trading_report.ipc"
)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| strategy_id | str | 策略唯一标识符 |
| market_addr | str | 行情通道地址 |
| order_addr | str | 订单通道地址 |
| report_addr | str | 回报通道地址 |

#### 生命周期方法

| 方法 | 说明 | 返回值 |
|------|------|--------|
| `start()` | 启动客户端，连接ZeroMQ通道 | bool |
| `stop()` | 停止客户端，断开连接 | None |
| `is_running()` | 检查是否在运行 | bool |
| `is_connected()` | 检查是否已连接 | bool |
| `run(strategy)` | 运行策略（主循环） | None |

#### 数据接收方法

| 方法 | 说明 | 返回值 |
|------|------|--------|
| `recv_ticker()` | 非阻塞接收行情 | Optional[TickerData] |
| `recv_report()` | 非阻塞接收回报 | Optional[OrderReport] |
| `recv_report_for_strategy(strategy_id)` | 接收指定策略的回报 | Optional[OrderReport] |

#### 订单发送方法

| 方法 | 说明 | 返回值 |
|------|------|--------|
| `send_order(order: OrderRequest)` | 发送订单请求 | bool |

---

### 3.2 Strategy 基类

所有策略都应继承此基类。

```python
from trading_client import Strategy, TickerData, OrderReport

class MyStrategy(Strategy):
    def on_ticker(self, data: TickerData):
        # 处理行情（必须实现）
        pass
    
    def on_order(self, report: OrderReport):
        # 处理订单回报（可选）
        pass
```

#### 生命周期回调

| 方法 | 调用时机 | 是否必须实现 |
|------|----------|--------------|
| `on_start()` | 策略启动时 | 否 |
| `on_stop()` | 策略停止时 | 否 |
| `on_ticker(data: TickerData)` | 收到行情时 | **是** |
| `on_order(report: OrderReport)` | 收到回报时 | 否 |

#### 下单便捷方法

| 方法 | 说明 | 参数 | 返回值 |
|------|------|------|--------|
| `buy_limit(symbol, quantity, price)` | 限价买入 | symbol: str, quantity: float, price: float | Optional[str] |
| `sell_limit(symbol, quantity, price)` | 限价卖出 | symbol: str, quantity: float, price: float | Optional[str] |
| `buy_market(symbol, quantity)` | 市价买入 | symbol: str, quantity: float | Optional[str] |
| `sell_market(symbol, quantity)` | 市价卖出 | symbol: str, quantity: float | Optional[str] |
| `send_order(order: OrderRequest)` | 发送自定义订单 | order: OrderRequest | Optional[str] |

#### 辅助方法

| 方法 | 说明 |
|------|------|
| `log(message: str)` | 打印带策略ID前缀的日志 |

#### 属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `strategy_id` | str | 策略ID |
| `position` | float | 当前持仓（需自己维护） |

---

### 3.3 OKX REST API 直接调用

Python端也可以直接调用OKX REST API，适用于需要更多控制的场景。

```python
from adapters.okx import OKXRestAPI

api = OKXRestAPI(
    api_key="your_api_key",
    secret_key="your_secret_key",
    passphrase="your_passphrase",
    is_demo=True  # 使用模拟盘
)
```

#### 交易接口

```python
# 下单
response = api.place_order(
    inst_id="BTC-USDT",      # 产品ID
    td_mode="cash",          # 交易模式: cash/isolated/cross
    side="buy",              # 方向: buy/sell
    ord_type="limit",        # 类型: market/limit/post_only/fok/ioc
    sz="0.001",              # 数量
    px="50000",              # 价格（限价单必填）
    cl_ord_id="my_order_1"   # 客户自定义订单ID（可选）
)

# 批量下单（最多20个）
orders = [
    {"instId": "BTC-USDT", "tdMode": "cash", "side": "buy", "ordType": "limit", "px": "50000", "sz": "0.01"},
    {"instId": "ETH-USDT", "tdMode": "cash", "side": "buy", "ordType": "limit", "px": "3000", "sz": "0.1"}
]
response = api.place_batch_orders(orders)

# 撤单
response = api.cancel_order(inst_id="BTC-USDT", ord_id="123456")

# 批量撤单
orders = [{"instId": "BTC-USDT", "ordId": "123456"}, {"instId": "ETH-USDT", "ordId": "789012"}]
response = api.cancel_batch_orders(orders)

# 修改订单
response = api.amend_order(inst_id="BTC-USDT", ord_id="123456", new_px="50100")
```

#### 查询接口

```python
# 查询订单
response = api.get_order(inst_id="BTC-USDT", ord_id="123456")

# 查询未成交订单
response = api.get_orders_pending(inst_type="SPOT", inst_id="BTC-USDT")

# 查询历史订单（7天内）
response = api.get_orders_history(inst_type="SPOT", state="filled")

# 查询账户余额
response = api.get_balance(ccy="USDT")

# 查询持仓
response = api.get_positions(inst_type="SWAP")

# 查询账单流水
response = api.get_bills(ccy="USDT", limit="50")
```

---

## 4. C++ REST API接口

### 4.1 OKXRestAPI 类

```cpp
#include "adapters/okx/okx_rest_api.h"

using namespace trading::okx;

OKXRestAPI api(
    "api_key",
    "secret_key",
    "passphrase",
    true  // is_testnet
);
```

### 4.2 下单接口

#### 简化版下单

```cpp
nlohmann::json place_order(
    const std::string& inst_id,    // 产品ID
    const std::string& td_mode,    // 交易模式
    const std::string& side,       // 方向: buy/sell
    const std::string& ord_type,   // 类型: market/limit
    double sz,                     // 数量
    double px = 0.0,               // 价格
    const std::string& cl_ord_id = ""  // 客户端订单ID
);
```

#### 完整版下单（支持止盈止损）

```cpp
PlaceOrderResponse place_order_advanced(const PlaceOrderRequest& request);
```

#### PlaceOrderRequest 结构

```cpp
struct PlaceOrderRequest {
    // 必填参数
    std::string inst_id;      // 产品ID
    std::string td_mode;      // 交易模式: cash/isolated/cross
    std::string side;         // 方向: buy/sell
    std::string ord_type;     // 类型: market/limit/post_only/fok/ioc
    std::string sz;           // 数量
    
    // 可选参数
    std::string ccy;          // 保证金币种
    std::string cl_ord_id;    // 客户自定义订单ID
    std::string tag;          // 订单标签
    std::string pos_side;     // 持仓方向: long/short
    std::string px;           // 价格
    bool reduce_only = false; // 是否只减仓
    
    // 止盈止损
    std::vector<AttachAlgoOrder> attach_algo_ords;
};
```

#### 带止盈止损下单

```cpp
PlaceOrderResponse place_order_with_tp_sl(
    const std::string& inst_id,
    const std::string& td_mode,
    const std::string& side,
    const std::string& ord_type,
    const std::string& sz,
    const std::string& px,
    const std::string& tp_trigger_px = "",  // 止盈触发价
    const std::string& tp_ord_px = "-1",    // 止盈委托价（-1=市价）
    const std::string& sl_trigger_px = "",  // 止损触发价
    const std::string& sl_ord_px = "-1",    // 止损委托价（-1=市价）
    const std::string& cl_ord_id = ""
);
```

### 4.3 撤单接口

```cpp
// 单个撤单
nlohmann::json cancel_order(
    const std::string& inst_id,
    const std::string& ord_id = "",
    const std::string& cl_ord_id = ""
);

// 批量撤单
nlohmann::json cancel_batch_orders(
    const std::vector<std::string>& ord_ids,
    const std::string& inst_id
);
```

### 4.4 查询接口

```cpp
// 查询订单
nlohmann::json get_order(
    const std::string& inst_id,
    const std::string& ord_id = "",
    const std::string& cl_ord_id = ""
);

// 查询未成交订单
nlohmann::json get_pending_orders(
    const std::string& inst_type = "",
    const std::string& inst_id = ""
);

// 查询账户余额
nlohmann::json get_account_balance(const std::string& ccy = "");

// 查询持仓
nlohmann::json get_positions(
    const std::string& inst_type = "",
    const std::string& inst_id = ""
);

// 查询可交易产品
nlohmann::json get_account_instruments(
    const std::string& inst_type,
    const std::string& inst_family = "",
    const std::string& inst_id = ""
);

// 查询K线
nlohmann::json get_candles(
    const std::string& inst_id,
    const std::string& bar,       // 时间粒度: 1m/5m/15m/1H/4H/1D
    int64_t after = 0,
    int64_t before = 0,
    int limit = 100
);
```

---

## 5. C++ WebSocket接口

### 5.1 OKXWebSocket 类

```cpp
#include "adapters/okx/okx_websocket.h"

using namespace trading::okx;
```

#### 端点类型

```cpp
enum class WsEndpointType {
    PUBLIC,    // 公共频道：行情、深度、成交
    BUSINESS,  // 业务频道：K线、trades-all
    PRIVATE    // 私有频道：订单、持仓、账户
};
```

#### 构造函数

```cpp
OKXWebSocket(
    const std::string& api_key = "",      // API密钥（私有频道需要）
    const std::string& secret_key = "",   // Secret密钥
    const std::string& passphrase = "",   // 密码短语
    bool is_testnet = false,              // 是否使用模拟盘
    WsEndpointType endpoint_type = WsEndpointType::PUBLIC  // 端点类型
);
```

#### 工厂函数（推荐使用）

```cpp
// 创建公共频道WebSocket（行情、深度、成交）
auto ws_public = create_public_ws(true);  // true = 模拟盘

// 创建业务频道WebSocket（K线、trades-all）
auto ws_business = create_business_ws(true);

// 创建私有频道WebSocket（订单、持仓、账户）
auto ws_private = create_private_ws(api_key, secret_key, passphrase, true);
```

### 5.2 回调类型

```cpp
using TickerCallback = std::function<void(const TickerData::Ptr&)>;
using TradeCallback = std::function<void(const TradeData::Ptr&)>;
using OrderBookCallback = std::function<void(const OrderBookData::Ptr&)>;
using KlineCallback = std::function<void(const KlineData::Ptr&)>;
using OrderCallback = std::function<void(const Order::Ptr&)>;
using RawMessageCallback = std::function<void(const nlohmann::json&)>;  // 调试用
```

### 5.3 公共频道订阅

```cpp
// 创建公共频道WebSocket
auto ws = create_public_ws(true);
ws->connect();

// 订阅行情快照
ws->subscribe_ticker("BTC-USDT");
ws->set_ticker_callback([](const TickerData::Ptr& ticker) {
    std::cout << "Price: " << ticker->last_price() << std::endl;
});

// 订阅逐笔成交
ws->subscribe_trades("BTC-USDT");
ws->set_trade_callback([](const TradeData::Ptr& trade) {
    std::cout << "Trade: " << trade->price() << " x " << trade->quantity() << std::endl;
});

// 订阅订单簿
ws->subscribe_orderbook("BTC-USDT", "books5");  // 5档深度
ws->set_orderbook_callback([](const OrderBookData::Ptr& book) {
    auto best_bid = book->best_bid();
    auto best_ask = book->best_ask();
    if (best_bid && best_ask) {
        std::cout << "Bid: " << best_bid->first << ", Ask: " << best_ask->first << std::endl;
    }
});
```

### 5.4 K线频道订阅（⚠️ 需要使用 BUSINESS 端点）

```cpp
// ⚠️ 重要：K线必须使用 business 端点！
auto ws_kline = create_business_ws(true);  // 创建 business 端点
ws_kline->connect();

// 设置K线回调
ws_kline->set_kline_callback([](const KlineData::Ptr& kline) {
    std::cout << "📊 K线: " << kline->symbol() 
              << " | " << kline->interval()
              << " | O:" << kline->open()
              << " H:" << kline->high()
              << " L:" << kline->low()
              << " C:" << kline->close()
              << " V:" << kline->volume()
              << std::endl;
});

// 方式1：使用枚举订阅
ws_kline->subscribe_kline("BTC-USDT", KlineInterval::MINUTE_1);   // 1分钟
ws_kline->subscribe_kline("BTC-USDT", KlineInterval::HOUR_1);     // 1小时
ws_kline->subscribe_kline("BTC-USDT", KlineInterval::DAY_1);      // 1天

// 方式2：使用字符串订阅
ws_kline->subscribe_kline("ETH-USDT", "5m");   // 5分钟
ws_kline->subscribe_kline("ETH-USDT", "1H");   // 1小时
ws_kline->subscribe_kline("ETH-USDT", "1D");   // 1天

// 取消订阅
ws_kline->unsubscribe_kline("BTC-USDT", KlineInterval::MINUTE_1);
ws_kline->unsubscribe_kline("ETH-USDT", "5m");
```

#### K线间隔枚举

```cpp
enum class KlineInterval {
    SECOND_1,   // 1秒    -> candle1s
    MINUTE_1,   // 1分钟  -> candle1m
    MINUTE_3,   // 3分钟  -> candle3m
    MINUTE_5,   // 5分钟  -> candle5m
    MINUTE_15,  // 15分钟 -> candle15m
    MINUTE_30,  // 30分钟 -> candle30m
    HOUR_1,     // 1小时  -> candle1H
    HOUR_2,     // 2小时  -> candle2H
    HOUR_4,     // 4小时  -> candle4H
    HOUR_6,     // 6小时  -> candle6H
    HOUR_12,    // 12小时 -> candle12H
    DAY_1,      // 1天    -> candle1D
    DAY_2,      // 2天    -> candle2D
    DAY_3,      // 3天    -> candle3D
    DAY_5,      // 5天    -> candle5D
    WEEK_1,     // 1周    -> candle1W
    MONTH_1,    // 1月    -> candle1M
    MONTH_3,    // 3月    -> candle3M
    // UTC时区版本
    DAY_1_UTC,  // -> candle1Dutc
    // ... 更多UTC版本
};
```

#### K线数据格式

```cpp
// KlineData 类属性
class KlineData {
    const std::string& symbol();     // 产品ID
    const std::string& interval();   // 时间间隔
    double open();                   // 开盘价
    double high();                   // 最高价
    double low();                    // 最低价
    double close();                  // 收盘价
    double volume();                 // 成交量
    std::optional<double> turnover(); // 成交额
    int64_t timestamp();             // 时间戳（毫秒）
};
```

### 5.5 私有频道订阅（需要登录）

私有频道需要先进行WebSocket登录认证。

#### 登录认证

```cpp
// 创建私有频道WebSocket
auto ws_private = create_private_ws(api_key, secret_key, passphrase, true);
ws_private->connect();
ws_private->login();  // 必须先登录！

// 检查登录状态
if (ws_private->is_logged_in()) {
    std::cout << "登录成功" << std::endl;
}
```

**登录签名算法：**
1. 获取当前Unix时间戳（秒级）
2. 拼接签名字符串：`timestamp + "GET" + "/users/self/verify"`
3. 使用 HMAC SHA256 + SecretKey 加密
4. Base64 编码

```cpp
// 内部实现示例
std::string create_signature(const std::string& timestamp) {
    std::string message = timestamp + "GET" + "/users/self/verify";
    // HMAC SHA256 + Base64
    return base64_encode(hmac_sha256(secret_key, message));
}
```

**登录请求格式：**
```json
{
    "op": "login",
    "args": [{
        "apiKey": "your_api_key",
        "passphrase": "your_passphrase",
        "timestamp": "1704876947",
        "sign": "base64_encoded_signature"
    }]
}
```

**登录响应：**
```json
// 成功
{"event": "login", "code": "0", "msg": "", "connId": "a4d3ae55"}

// 失败
{"event": "error", "code": "60009", "msg": "Login failed.", "connId": "a4d3ae55"}
```

#### 订阅订单频道

```cpp
// 订阅所有SPOT订单
ws_private->subscribe_orders("SPOT");

// 订阅指定产品的订单
ws_private->subscribe_orders("SPOT", "BTC-USDT");

// 订阅指定交易品种的订单（适用于衍生品）
ws_private->subscribe_orders("SWAP", "", "BTC-USD");

// 设置回调
ws_private->set_order_callback([](const Order::Ptr& order) {
    std::cout << "订单ID: " << order->exchange_order_id() 
              << " 状态: " << order_state_to_string(order->state())
              << " 已成交: " << order->filled_quantity()
              << std::endl;
});
```

**订阅参数：**
| 参数 | 类型 | 是否必须 | 说明 |
|------|------|----------|------|
| instType | String | 是 | 产品类型：SPOT/MARGIN/SWAP/FUTURES/OPTION/ANY |
| instId | String | 否 | 产品ID，如 "BTC-USDT" |
| instFamily | String | 否 | 交易品种（适用于衍生品） |

#### 订阅持仓频道

```cpp
// 订阅所有SWAP持仓
ws_private->subscribe_positions("SWAP");

// 订阅指定产品的持仓
ws_private->subscribe_positions("SWAP", "BTC-USDT-SWAP");

// 设置回调（接收原始JSON）
ws_private->set_position_callback([](const nlohmann::json& pos_data) {
    std::cout << "持仓更新: " << pos_data.dump() << std::endl;
    // 可解析的字段:
    // - instId: 产品ID
    // - posSide: 持仓方向 (long/short/net)
    // - pos: 持仓数量
    // - avgPx: 平均开仓价格
    // - upl: 未实现盈亏
    // - lever: 杠杆倍数
    // - liqPx: 预估强平价格
});
```

#### 订阅账户频道

```cpp
// 订阅所有币种账户更新
ws_private->subscribe_account();

// 订阅指定币种账户更新
ws_private->subscribe_account("USDT");

// 设置回调（接收原始JSON）
ws_private->set_account_callback([](const nlohmann::json& acc_data) {
    std::cout << "账户更新: " << acc_data.dump() << std::endl;
    // 可解析的字段:
    // - totalEq: 总权益（美元）
    // - adjEq: 有效保证金
    // - imr: 初始保证金
    // - mmr: 维持保证金
    // - mgnRatio: 保证金率
    // - details: 各币种详情数组
});
```

#### 取消订阅

```cpp
// 取消订阅订单
ws_private->unsubscribe_orders("SPOT");
ws_private->unsubscribe_orders("SPOT", "BTC-USDT");

// 取消订阅持仓
ws_private->unsubscribe_positions("SWAP");

// 取消订阅账户
ws_private->unsubscribe_account();
```

### 5.6 完整示例：私有频道（订单/持仓/账户）

```cpp
#include "adapters/okx/okx_websocket.h"
#include <iostream>
#include <thread>
#include <chrono>

int main() {
    using namespace trading::okx;
    
    // API凭证
    const std::string api_key = "your_api_key";
    const std::string secret_key = "your_secret_key";
    const std::string passphrase = "your_passphrase";
    
    // 1. 创建私有频道WebSocket
    auto ws = create_private_ws(api_key, secret_key, passphrase, true);  // true = 模拟盘
    
    // 2. 设置回调
    ws->set_order_callback([](const Order::Ptr& order) {
        std::cout << "📦 订单更新: "
                  << order->symbol() << " | "
                  << (order->side() == OrderSide::BUY ? "买" : "卖") << " | "
                  << order->quantity() << " @ " << order->price() << " | "
                  << "状态: " << order_state_to_string(order->state()) << " | "
                  << "已成交: " << order->filled_quantity()
                  << std::endl;
    });
    
    ws->set_position_callback([](const nlohmann::json& pos) {
        std::cout << "📊 持仓更新: " << pos.dump(2) << std::endl;
    });
    
    ws->set_account_callback([](const nlohmann::json& acc) {
        std::cout << "💰 账户更新: totalEq=" << acc.value("totalEq", "N/A") << std::endl;
    });
    
    // 3. 连接
    if (!ws->connect()) {
        std::cerr << "连接失败" << std::endl;
        return 1;
    }
    
    // 4. 登录（私有频道必须）
    ws->login();
    std::this_thread::sleep_for(std::chrono::seconds(2));  // 等待登录完成
    
    if (!ws->is_logged_in()) {
        std::cerr << "登录失败" << std::endl;
        return 1;
    }
    
    // 5. 订阅私有频道
    ws->subscribe_orders("SPOT");        // 订阅现货订单
    ws->subscribe_positions("SWAP");     // 订阅合约持仓
    ws->subscribe_account();             // 订阅账户
    
    // 6. 保持运行
    std::cout << "等待推送数据..." << std::endl;
    std::this_thread::sleep_for(std::chrono::minutes(10));
    
    // 7. 清理
    ws->unsubscribe_orders("SPOT");
    ws->unsubscribe_positions("SWAP");
    ws->unsubscribe_account();
    ws->disconnect();
    
    return 0;
}
```

### 5.7 完整示例：订阅K线数据

```cpp
#include "adapters/okx/okx_websocket.h"
#include <iostream>
#include <thread>
#include <chrono>

int main() {
    using namespace trading::okx;
    
    // 1. 创建 business 端点的 WebSocket（K线必须使用此端点）
    auto ws = create_business_ws(true);  // true = 模拟盘
    
    // 2. 设置K线回调
    ws->set_kline_callback([](const KlineData::Ptr& kline) {
        std::cout << "📊 " << kline->symbol() 
                  << " [" << kline->interval() << "]"
                  << " O:" << kline->open()
                  << " H:" << kline->high()
                  << " L:" << kline->low()
                  << " C:" << kline->close()
                  << " V:" << kline->volume()
                  << std::endl;
    });
    
    // 3. 连接
    if (!ws->connect()) {
        std::cerr << "连接失败" << std::endl;
        return 1;
    }
    
    // 4. 订阅K线
    ws->subscribe_kline("BTC-USDT", KlineInterval::MINUTE_1);
    ws->subscribe_kline("ETH-USDT", "1H");
    
    // 5. 保持运行
    std::this_thread::sleep_for(std::chrono::minutes(5));
    
    // 6. 清理
    ws->unsubscribe_kline("BTC-USDT", KlineInterval::MINUTE_1);
    ws->disconnect();
    
    return 0;
}
```

---

## 6. ZeroMQ通信协议

### 6.1 IPC地址

```cpp
// C++ 端
struct IpcAddresses {
    static constexpr const char* MARKET_DATA = "ipc:///tmp/trading_md.ipc";
    static constexpr const char* ORDER = "ipc:///tmp/trading_order.ipc";
    static constexpr const char* REPORT = "ipc:///tmp/trading_report.ipc";
};
```

```python
# Python 端
class IpcAddresses:
    MARKET_DATA = "ipc:///tmp/trading_md.ipc"
    ORDER = "ipc:///tmp/trading_order.ipc"
    REPORT = "ipc:///tmp/trading_report.ipc"
```

### 6.2 消息格式

#### 行情消息 (服务端 → 策略端)

```json
{
    "type": "ticker",
    "symbol": "BTC-USDT",
    "last_price": 43250.5,
    "bid_price": 43250.0,
    "ask_price": 43251.0,
    "bid_size": 1.5,
    "ask_size": 2.0,
    "volume_24h": 10000.0,
    "timestamp": 1702000000000,
    "timestamp_ns": 1702000000000000000,
    "seq_num": 12345
}
```

#### 订单请求 (策略端 → 服务端)

```json
{
    "type": "order_request",
    "strategy_id": "my_strategy",
    "client_order_id": "mystrategy17020000001234",
    "symbol": "BTC-USDT",
    "side": "buy",
    "order_type": "limit",
    "price": 43000.0,
    "quantity": 0.001,
    "timestamp": 1702000000000
}
```

#### 订单回报 (服务端 → 策略端)

```json
{
    "type": "order_report",
    "strategy_id": "my_strategy",
    "client_order_id": "mystrategy17020000001234",
    "exchange_order_id": "1234567890",
    "symbol": "BTC-USDT",
    "status": "accepted",
    "filled_price": 43000.0,
    "filled_quantity": 0.001,
    "fee": 0.00001,
    "error_msg": "",
    "timestamp": 1702000000100,
    "timestamp_ns": 1702000000100000000
}
```

### 6.3 订单状态

| 状态 | 说明 |
|------|------|
| `accepted` | 交易所已接受 |
| `filled` | 完全成交 |
| `partial` | 部分成交 |
| `cancelled` | 已取消 |
| `rejected` | 被拒绝 |

---

## 7. 数据结构定义

### 7.1 TickerData (行情快照)

| 字段 | 类型 | 说明 |
|------|------|------|
| symbol | string | 交易对 |
| last_price | float | 最新价 |
| bid_price | float | 买一价 |
| ask_price | float | 卖一价 |
| bid_size | float | 买一量 |
| ask_size | float | 卖一量 |
| volume_24h | float | 24小时成交量 |
| timestamp | int64 | 时间戳（毫秒） |
| timestamp_ns | int64 | 时间戳（纳秒） |

#### 计算属性

| 方法 | 说明 |
|------|------|
| `spread()` | 买卖价差 = ask_price - bid_price |
| `spread_bps()` | 价差基点 = spread / mid_price * 10000 |
| `mid_price()` | 中间价 = (bid + ask) / 2 |

### 7.2 OrderRequest (订单请求)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| strategy_id | string | 是 | 策略ID |
| symbol | string | 是 | 交易对 |
| side | string | 是 | 方向: buy/sell |
| order_type | string | 否 | 类型: limit/market，默认limit |
| price | float | 限价单必填 | 价格 |
| quantity | float | 是 | 数量 |
| client_order_id | string | 否 | 自动生成 |

### 7.3 OrderReport (订单回报)

| 字段 | 类型 | 说明 |
|------|------|------|
| strategy_id | string | 策略ID |
| client_order_id | string | 客户端订单ID |
| exchange_order_id | string | 交易所订单ID |
| symbol | string | 交易对 |
| status | string | 订单状态 |
| filled_price | float | 成交价 |
| filled_quantity | float | 成交量 |
| fee | float | 手续费 |
| error_msg | string | 错误信息 |
| timestamp | int64 | 时间戳（毫秒） |
| timestamp_ns | int64 | 时间戳（纳秒） |

#### 便捷方法

| 方法 | 说明 |
|------|------|
| `is_success()` | 是否成功（accepted/filled/partial） |
| `is_filled()` | 是否完全成交 |
| `is_rejected()` | 是否被拒绝 |

---

## 8. 使用示例

### 8.1 Python 策略示例

```python
from trading_client import TradingClient, Strategy, TickerData, OrderReport

class SimpleStrategy(Strategy):
    """简单突破策略"""
    
    def __init__(self):
        super().__init__()
        self.high_price = 0.0
        self.order_placed = False
    
    def on_start(self):
        self.log("策略启动，等待突破信号...")
    
    def on_ticker(self, data: TickerData):
        # 更新最高价
        if data.last_price > self.high_price:
            self.high_price = data.last_price
        
        # 价格突破时买入
        if not self.order_placed and data.last_price > self.high_price * 0.99:
            order_id = self.buy_limit(
                symbol=data.symbol,
                quantity=0.001,
                price=data.bid_price  # 以买一价挂单
            )
            if order_id:
                self.order_placed = True
                self.log(f"下单成功: {order_id}")
    
    def on_order(self, report: OrderReport):
        if report.is_filled():
            self.position += report.filled_quantity
            self.log(f"成交！持仓: {self.position}")
        elif report.is_rejected():
            self.log(f"订单被拒: {report.error_msg}")
            self.order_placed = False

if __name__ == "__main__":
    client = TradingClient(strategy_id="simple_breakout")
    client.run(SimpleStrategy())
```

### 8.2 C++ 下单示例

```cpp
#include "adapters/okx/okx_rest_api.h"
#include <iostream>

int main() {
    using namespace trading::okx;
    
    // 创建API客户端
    OKXRestAPI api("api_key", "secret_key", "passphrase", true);
    
    // 简单下单
    auto response = api.place_order(
        "BTC-USDT",   // inst_id
        "cash",       // td_mode
        "buy",        // side
        "limit",      // ord_type
        0.001,        // sz
        43000.0,      // px
        "my_order_1"  // cl_ord_id
    );
    
    if (response["code"] == "0") {
        auto& data = response["data"][0];
        std::cout << "Order placed: " << data["ordId"] << std::endl;
    } else {
        std::cout << "Order failed: " << response["msg"] << std::endl;
    }
    
    // 带止盈止损的下单
    auto result = api.place_order_with_tp_sl(
        "BTC-USDT",   // inst_id
        "cash",       // td_mode
        "buy",        // side
        "limit",      // ord_type
        "0.001",      // sz
        "43000",      // px
        "44000",      // tp_trigger_px (止盈触发价)
        "-1",         // tp_ord_px (市价止盈)
        "42000",      // sl_trigger_px (止损触发价)
        "-1"          // sl_ord_px (市价止损)
    );
    
    if (result.is_success()) {
        std::cout << "Order with TP/SL placed: " << result.ord_id << std::endl;
    }
    
    return 0;
}
```

### 8.3 启动流程

```bash
# 1. 先启动C++交易服务器
cd cpp/build
./trading_server

# 2. 然后启动Python策略
cd python
python -c "
from trading_client import TradingClient, Strategy, TickerData

class MyStrategy(Strategy):
    def on_ticker(self, data: TickerData):
        print(f'收到行情: {data.symbol} @ {data.last_price}')

client = TradingClient(strategy_id='test')
client.run(MyStrategy())
"
```

---

## 附录: 待实现接口

以下OKX接口尚未实现，后续可根据需要添加：

### 交易接口
- [ ] 一键平仓 (POST /api/v5/trade/close-position)
- [ ] 策略委托下单 (POST /api/v5/trade/order-algo)
- [ ] 撤销策略委托 (POST /api/v5/trade/cancel-algos)
- [ ] 获取成交明细 (GET /api/v5/trade/fills)

### 账户接口
- [ ] 账户配置 (GET /api/v5/account/config)
- [ ] 设置杠杆 (POST /api/v5/account/set-leverage)
- [ ] 账户最大可买卖 (GET /api/v5/account/max-size)
- [ ] 账户最大可开仓数量 (GET /api/v5/account/max-avail-size)

### 行情接口
- [ ] 深度数据 (GET /api/v5/market/books)
- [ ] 历史成交 (GET /api/v5/market/trades)
- [ ] 指数行情 (GET /api/v5/market/index-tickers)

### WebSocket频道
- [ ] 深度快照频道 (books5)
- [ ] 资金费率频道 (funding-rate)
- [ ] 标记价格频道 (mark-price)
- [ ] 指数行情频道 (index-tickers)

