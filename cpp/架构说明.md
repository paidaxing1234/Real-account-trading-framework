# C++ 实盘交易框架 - 架构说明

## 📊 项目概述

本项目是基于C++17的高性能实盘交易框架，参考Python版本的设计理念，采用事件驱动架构和组件化设计，对接OKX交易所API。

## 🎯 设计理念

### 1. 与Python版本保持一致的架构

C++版本完全遵循Python版本的架构设计：

- ✅ 事件驱动架构（EventEngine）
- ✅ 组件化设计（Component）
- ✅ 相同的数据模型（Order, Data）
- ✅ 相同的模块划分（core, adapters, strategies, utils）

### 2. C++的优势

同时充分利用C++的特性：

- 🚀 **高性能**：编译执行，性能比Python高10-100倍
- 🔒 **类型安全**：编译时类型检查，减少运行时错误
- 💾 **内存管理**：智能指针自动管理，RAII模式
- 🧵 **真多线程**：无GIL限制，充分利用多核
- 📦 **单一部署**：编译为可执行文件，无需运行时环境

## 🏗️ 核心架构

### 事件引擎（EventEngine）

**职责**：
1. 接收事件（put方法）
2. 分发事件给注册的监听器
3. 管理全局时间戳
4. 支持动态接口注入

**核心代码**：

```cpp
class EventEngine {
public:
    // 注册类型化监听器
    void register_listener(
        const std::type_index& event_type,
        Listener listener,
        bool ignore_self = true
    );
    
    // 注册全局监听器
    void register_global_listener(
        Listener listener,
        bool ignore_self = false,
        bool is_senior = false
    );
    
    // 推送事件
    void put(Event::Ptr event);
    
    // 动态接口注入
    template<typename Func>
    void inject(const std::string& name, Func func);
    
    template<typename RetType, typename... Args>
    RetType call(const std::string& name, Args&&... args);
};
```

**特性**：
- 使用 `std::type_index` 实现类型化监听
- 使用 `std::function` 封装监听器
- 使用 `std::any` 实现动态接口注入
- 使用队列保证事件顺序处理

### 事件基类（Event）

**设计**：
- 所有事件都继承自 `Event`
- 使用 `std::shared_ptr` 管理生命周期
- 包含时间戳、来源、产生者信息

```cpp
class Event {
public:
    using Ptr = std::shared_ptr<Event>;
    
    int64_t timestamp() const;
    const EventEngine* source() const;
    size_t producer_id() const;
    
    virtual std::string type_name() const { return "Event"; }
    virtual Event::Ptr copy() const;
    virtual Event::Ptr derive() const;
};
```

**派生类**：
- `Order` - 订单事件
- `Data` - 数据基类
  - `TickerData` - 行情快照
  - `TradeData` - 逐笔成交
  - `OrderBookData` - 订单簿
  - `KlineData` - K线数据

### 组件基类（Component）

**职责**：
- 定义统一的生命周期接口
- start(): 启动组件，注册监听器
- stop(): 停止组件，清理资源

```cpp
class Component {
public:
    virtual void start(EventEngine* engine) = 0;
    virtual void stop() = 0;
    
protected:
    EventEngine* engine_ = nullptr;
};
```

**实现类**：
- `OKXAdapter` - OKX适配器
- `StrategyBase` - 策略基类
- `AccountManager` - 账户管理
- `Recorder` - 数据记录

## 📦 模块详解

### Core（核心模块）

#### event.h - 事件基类
- 提供事件的基础接口
- 时间戳、来源、产生者管理
- 浅拷贝和派生方法

#### event_engine.h - 事件引擎
- 事件注册和分发
- 全局时间戳管理
- 动态接口注入
- Component抽象基类

#### order.h - 订单模型
- 订单类型枚举（LIMIT, MARKET, POST_ONLY等）
- 订单方向枚举（BUY, SELL）
- 订单状态枚举（CREATED → SUBMITTED → FILLED等）
- 订单属性和便捷方法
- 工厂方法（buy_limit, sell_market等）

#### data.h - 数据模型
- Data基类
- TickerData：行情快照（最新价、买卖价、成交量）
- TradeData：逐笔成交
- OrderBookData：订单簿（买卖盘）
- KlineData：K线数据（OHLCV）

### Adapters（适配器）

#### okx_adapter.h - OKX适配器组件
- 监听内部Order事件 → 调用REST API下单
- 接收WebSocket推送 → 转换为内部Event
- 维护订单映射（本地ID ↔ 交易所ID）

#### okx_rest_api.h - REST API封装
- 下单（place_order）
- 撤单（cancel_order）
- 查询订单（get_order）
- 查询账户（get_account_balance）
- 查询持仓（get_positions）

#### okx_websocket.h - WebSocket连接
- 连接和登录认证
- 订阅公共频道（ticker, trades, orderbook, kline）
- 订阅私有频道（orders, positions, account）
- 心跳保持和自动重连
- 消息解析和回调

### Strategies（策略）

#### strategy_base.h - 策略基类
- 提供统一的策略接口
- on_init(): 策略初始化
- on_ticker(): 行情回调
- on_order(): 订单回调
- send_order(): 发送订单
- buy()/sell(): 便捷下单方法

#### demo_strategy.h - 示例策略
- 简单网格策略
- 演示如何使用框架
- 展示订单管理和持仓跟踪

### Utils（工具）

#### account_manager.h - 账户管理
- 维护活跃订单字典
- 维护持仓字典
- 计算未实现/已实现盈亏
- 向引擎注入查询接口

#### recorder.h - 数据记录
- 监听所有事件（全局监听器）
- 记录订单成交
- 写入日志文件

## 🔄 数据流向

```
┌──────────────────────────────────────────────────────────┐
│                      实盘数据流                            │
└──────────────────────────────────────────────────────────┘

OKX交易所
    ↓ WebSocket推送
OKXWebSocket::on_message()
    ↓ 解析消息，创建TickerData/Order
OKXAdapter::on_ticker_update()
    ↓ engine->put(ticker)
EventEngine::drain()
    ↓ 分发给所有监听器
    ├→ Strategy::on_ticker()         # 策略收到行情
    ├→ AccountManager::on_ticker()   # 账户更新价格
    └→ Recorder::on_event()          # 记录器记录数据

Strategy判断后发出订单
    ↓ auto order = buy_limit(...)
Strategy::send_order(order)
    ↓ engine->put(order)
EventEngine::drain()
    ↓ 分发
    ├→ AccountManager::on_order()    # 账户更新订单状态
    ├→ OKXAdapter::on_order_event()  # 适配器发送到交易所
    └→ Recorder::on_event()          # 记录器记录订单

OKXAdapter::submit_order()
    ↓ rest_api->place_order()
    ↓ HTTP请求到OKX
OKX交易所返回
    ↓ WebSocket推送订单更新
OKXAdapter::on_order_update()
    ↓ 更新订单状态为FILLED
    ↓ engine->put(order)
EventEngine::drain()
    ↓ 分发
    ├→ Strategy::on_order()          # 策略知道成交了
    ├→ AccountManager::on_order()    # 账户更新持仓
    └→ Recorder::on_event()          # 记录成交
```

## 🎓 C++特性的应用

### 1. 智能指针

使用 `shared_ptr` 管理事件生命周期：

```cpp
using Ptr = std::shared_ptr<Event>;

auto order = std::make_shared<Order>(...);
engine->put(order);  // 引用计数+1
// 离开作用域，自动释放
```

### 2. 类型索引

使用 `std::type_index` 实现类型化监听：

```cpp
engine->register_listener(typeid(Order), [](const Event::Ptr& e) {
    auto order = std::dynamic_pointer_cast<Order>(e);
    // 处理订单
});
```

### 3. 函数对象

使用 `std::function` 封装监听器：

```cpp
using Listener = std::function<void(const Event::Ptr&)>;

Listener listener = [this](const Event::Ptr& e) {
    this->on_order(std::dynamic_pointer_cast<Order>(e));
};
```

### 4. RAII

组件自动清理资源：

```cpp
class Recorder : public Component {
public:
    ~Recorder() {
        close_log();  // 自动关闭日志文件
    }
};
```

### 5. std::optional

用于可选值：

```cpp
std::optional<double> bid_price() const { return bid_price_; }

auto mid = ticker->mid_price();
if (mid) {
    double price = *mid;
}
```

### 6. 模板方法

策略基类使用模板方法模式：

```cpp
class StrategyBase : public Component {
public:
    virtual void start(EventEngine* engine) override final {
        // 框架代码
        on_init();  // 调用子类实现
    }
    
    virtual void on_init() {}  // 子类可重写
};
```

## 🔒 线程安全

### 当前实现

- EventEngine：单线程事件处理（队列顺序处理）
- AccountManager：使用 `std::mutex` 保护共享数据

### 多线程扩展

如需多线程，可以：
1. 使用线程安全队列（如 `tbb::concurrent_queue`）
2. 为关键数据结构加锁（`std::mutex`）
3. 使用无锁数据结构（`std::atomic`）

```cpp
class AccountManager {
private:
    std::mutex mutex_;
    std::map<int64_t, Order::Ptr> orders_;
    
public:
    void add_order(const Order::Ptr& order) {
        std::lock_guard<std::mutex> lock(mutex_);
        orders_[order->order_id()] = order;
    }
};
```

## 📊 性能优化

### 1. Header-Only设计

核心模块使用header-only，编译器可以充分优化：
- 内联函数
- 模板展开
- 零开销抽象

### 2. 移动语义

避免不必要的拷贝：

```cpp
void put(Event::Ptr event) {
    queue_.push(std::move(event));  // 移动而非拷贝
}
```

### 3. 对象池

可以使用对象池减少内存分配：

```cpp
class OrderPool {
    std::vector<std::unique_ptr<Order>> pool_;
public:
    Order* acquire();
    void release(Order* order);
};
```

### 4. 内存布局

使用 `__slots__` 风格的设计减少内存占用（Python中的概念，C++中通过紧凑的类设计实现）。

## 🚀 部署和运行

### 编译优化

```bash
# Release模式编译
cmake -DCMAKE_BUILD_TYPE=Release ..
cmake --build .

# 启用LTO（链接时优化）
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_INTERPROCEDURAL_OPTIMIZATION=ON ..
```

### 静态链接

```bash
# 静态链接所有库（便于部署）
cmake -DBUILD_SHARED_LIBS=OFF ..
```

### 运行

```bash
# 直接运行
./main_example

# 后台运行
nohup ./main_example > output.log 2>&1 &

# 使用systemd（Linux）
sudo systemctl start trading-bot
```

## 📝 开发建议

### 1. 代码风格

- 遵循Google C++ Style Guide
- 使用现代C++特性（C++17）
- 避免裸指针，使用智能指针

### 2. 错误处理

```cpp
try {
    // 交易逻辑
} catch (const std::exception& e) {
    log_error(e.what());
    // 优雅降级
}
```

### 3. 日志记录

```cpp
void log_info(const std::string& message) {
    auto ts = Event::current_timestamp();
    printf("[%ld] [INFO] %s\n", ts, message.c_str());
}
```

### 4. 单元测试

使用Google Test框架：

```cpp
TEST(EventEngineTest, RegisterAndPut) {
    EventEngine engine;
    bool called = false;
    
    engine.register_listener(typeid(Event), [&](const Event::Ptr& e) {
        called = true;
    });
    
    auto event = std::make_shared<Event>();
    engine.put(event);
    
    EXPECT_TRUE(called);
}
```

## 🔮 未来扩展

### 1. 多交易所支持

```cpp
class BinanceAdapter : public Component { ... };
class HuobiAdapter : public Component { ... };
```

### 2. 策略热重载

使用动态链接库（.so/.dll）实现策略热重载。

### 3. 分布式部署

- 使用ZeroMQ/nanomsg进行进程间通信
- 事件引擎作为中心节点
- 策略作为独立进程

### 4. Web界面

- 使用crow/beast提供HTTP API
- 实时推送WebSocket
- Vue.js前端展示

## 📞 总结

C++版本在保持Python版本架构设计的同时，充分利用了C++的性能和类型安全优势，适合对性能有高要求的实盘交易场景。

**核心优势**：
- ✅ 事件驱动 + 组件化 = 高扩展性
- ✅ 智能指针 + RAII = 安全的内存管理
- ✅ 编译执行 + 模板优化 = 极致性能
- ✅ Header-Only = 易于集成和部署

---

**版本**：v1.0.0  
**更新时间**：2025-12-08  
**作者**：参考Python版本设计

